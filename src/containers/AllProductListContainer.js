import AllProductList from "../components/AllProductList";

function AllProductListContainer({ products }) {
  return <AllProductList products={products} />;
}

export default AllProductListContainer;
